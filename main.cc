#include <iostream>
#include <string>
#include "grid.h"
using namespace std;

int main() {
    string cmd;
    Grid g;

    while(cin >> cmd) {
        if (cmd == game) {

        } else if (cmd == move) {

        } else if (cmd == setup) {

        } else if (cmd == move)
    }

}